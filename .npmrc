# Workspace-wide flat node_modules.
# Required for Expo + React Native (artifacts/mobile) to find every transitive
# dep without manual installs. Other artifacts (web/api) work fine with this
# layout too — it just uses more disk than the default symlinked mode.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
